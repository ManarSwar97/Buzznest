<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link rel="stylesheet" href="/stylesheets/style6.css" />

</head>
<body>
    <%- include('../navbar/_navbar.ejs')%>
    <div class="new-form">
        <div class="form">
    <h3>Edit the Post</h3>
    <form action="/posts/<%= posts._id %>?_method=PUT" method="POST">
        <div class="form-group">
        <label for="postTitle">Add Post Title</label>
        <input type="text" name="postTitle" id="postTitle" value="<%= posts.postTitle %>">
        <br>
        </div>
        <div class="form-group">
        <label for="postText">Add Post Description</label>
        <textarea name="postText" id="postText" placeholder="Game Description"><%= posts.postText %></textarea>
        <br>
        </div>
        <div class="form-group">
        <label for="postImage">Add Post Image</label>
        <input type="file" id="postImage" name="postImage" value="<%= posts.postImage %>"/>
        <br>
        </div>
        <div class="button-container">
        <button type="submit">Edit Post</button>
        </div>
    </form>
    </div>
</div>

</body>
</html>